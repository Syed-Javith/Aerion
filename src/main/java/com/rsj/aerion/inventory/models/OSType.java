package com.rsj.aerion.inventory.models;

public enum OSType {
    CENTOS("CentOS"),
    DEBIAN("Debian"),
    FREEBSD("FreeBSD"),
    WINDOWS("Windows"),
    MACOS("macOS");

    OSType(String  type) {
    }
}
